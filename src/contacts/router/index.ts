import ContactsView from '../views/ContactsView.vue'

const ContactsRoute = {
  path: '/',
  name: 'contacts',
  component: ContactsView,
  meta: {
    title: 'Contacts'
  }
}

export default ContactsRoute
